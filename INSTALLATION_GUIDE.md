# Ekzor Comments AutoSend Bot
## Полная инструкция по установке и настройке

---

## 📋 Описание

Этот бот автоматически отправляет комментарий с картинкой и кнопками под каждый новый пост в вашем Telegram канале.

**Возможности:**
- Автоматическое отслеживание новых постов
- Отправка комментария с картинкой
- Кнопки с ссылками (чат, музыка и т.д.)
- Работа в фоне как системная служба
- Автоматический перезапуск при сбоях

---

## 🔧 Подготовка

### 1. Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например: "Ekzor Comments Bot")
4. Придумайте username (должен заканчиваться на "bot", например: "ekzor_comments_bot")
5. Сохраните полученный **токен** - он понадобится позже

### 2. Добавление бота в канал

1. Откройте настройки вашего канала
2. Перейдите в раздел "Администраторы"
3. Нажмите "Добавить администратора"
4. Найдите вашего бота и добавьте его
5. **ВАЖНО:** Дайте боту следующие права:
   - ✅ Публикация сообщений
   - ✅ Редактирование сообщений
   - ✅ Удаление сообщений

### 3. Включение комментариев в канале

1. Откройте настройки канала
2. Найдите раздел "Обсуждения"
3. Создайте группу обсуждений или привяжите существующую
4. **Без этого шага комментарии работать не будут!**

### 4. Получение ID канала

Есть два способа:

**Способ 1 (через username):**
Если у вашего канала есть публичное имя, используйте формат: `@channelname`

**Способ 2 (через ID):**
1. Перешлите любое сообщение из канала боту [@userinfobot](https://t.me/userinfobot)
2. Он покажет ID канала в формате `-1001234567890`

---

## 🚀 Установка на VDS

### Вариант 1: Автоматическая установка (рекомендуется)

1. Подключитесь к серверу по SSH:
```bash
ssh root@your_server_ip
```

2. Скачайте файлы на сервер:
```bash
cd ~
# Скопируйте все файлы бота на сервер через scp или wget
```

3. Запустите скрипт установки:
```bash
chmod +x install.sh
sudo ./install.sh
```

4. Следуйте инструкциям на экране

### Вариант 2: Ручная установка

1. Подключитесь к серверу:
```bash
ssh root@your_server_ip
```

2. Обновите систему:
```bash
apt-get update
apt-get upgrade -y
```

3. Установите Python и необходимые пакеты:
```bash
apt-get install -y python3 python3-pip python3-venv
```

4. Создайте пользователя для бота:
```bash
useradd -r -s /bin/false telegram-bot
```

5. Создайте директорию для бота:
```bash
mkdir -p /opt/ekzor_comments_autosend
cd /opt/ekzor_comments_autosend
```

6. Скопируйте файлы бота:
```bash
# Разместите файлы ekzor_comments_autosend.py и requirements.txt в /opt/ekzor_comments_autosend/
```

7. Создайте виртуальное окружение:
```bash
python3 -m venv /opt/ekzor_comments_autosend/venv
```

8. Установите зависимости:
```bash
/opt/ekzor_comments_autosend/venv/bin/pip install --upgrade pip
/opt/ekzor_comments_autosend/venv/bin/pip install -r requirements.txt
```

9. Создайте файл конфигурации:
```bash
nano /opt/ekzor_comments_autosend/.env
```

Вставьте следующее (замените своими данными):
```bash
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
CHANNEL_ID=@your_channel
CHAT_URL=https://t.me/your_chat
MUSIC_URL=https://music.yandex.ru/users/your_playlist
PHOTO_PATH=/opt/ekzor_comments_autosend/image.jpg
COMMENT_TEXT=🎵 Слушайте нашу музыку и общайтесь с нами!
```

10. Добавьте картинку:
```bash
# Скопируйте вашу картинку
cp /path/to/your/image.jpg /opt/ekzor_comments_autosend/image.jpg
```

11. Установите права:
```bash
chown -R telegram-bot:telegram-bot /opt/ekzor_comments_autosend
chmod +x /opt/ekzor_comments_autosend/ekzor_comments_autosend.py
mkdir -p /var/log
touch /var/log/ekzor_comments_autosend.log
chown telegram-bot:telegram-bot /var/log/ekzor_comments_autosend.log
```

12. Создайте systemd service:
```bash
nano /etc/systemd/system/ekzor_comments_autosend.service
```

Вставьте содержимое файла `ekzor_comments_autosend.service`

13. Обновите путь к Python в первой строке бота:
```bash
sed -i '1s|.*|#!/opt/ekzor_comments_autosend/venv/bin/python3|' /opt/ekzor_comments_autosend/ekzor_comments_autosend.py
```

14. Обновите путь в service файле:
```bash
sed -i "s|ExecStart=/usr/bin/python3|ExecStart=/opt/ekzor_comments_autosend/venv/bin/python3|" /etc/systemd/system/ekzor_comments_autosend.service
```

15. Загрузите конфигурацию systemd:
```bash
systemctl daemon-reload
```

---

## ⚙️ Настройка

### Редактирование конфигурации

```bash
nano /opt/ekzor_comments_autosend/.env
```

**Параметры:**

- `BOT_TOKEN` - токен вашего бота от @BotFather
- `CHANNEL_ID` - ID или username канала (например: @mychannel или -1001234567890)
- `CHAT_URL` - ссылка на чат для кнопки
- `MUSIC_URL` - ссылка на музыку для кнопки
- `PHOTO_PATH` - путь к картинке (по умолчанию: /opt/ekzor_comments_autosend/image.jpg)
- `COMMENT_TEXT` - текст комментария (можно использовать HTML теги)

### Замена картинки

```bash
cp new_image.jpg /opt/ekzor_comments_autosend/image.jpg
chown telegram-bot:telegram-bot /opt/ekzor_comments_autosend/image.jpg
systemctl restart ekzor_comments_autosend
```

---

## 🎮 Управление ботом

### Запуск бота
```bash
systemctl start ekzor_comments_autosend
```

### Остановка бота
```bash
systemctl stop ekzor_comments_autosend
```

### Перезапуск бота
```bash
systemctl restart ekzor_comments_autosend
```

### Добавление в автозагрузку
```bash
systemctl enable ekzor_comments_autosend
```

### Удаление из автозагрузки
```bash
systemctl disable ekzor_comments_autosend
```

### Проверка статуса
```bash
systemctl status ekzor_comments_autosend
```

### Просмотр логов
```bash
# Последние 50 строк
journalctl -u ekzor_comments_autosend -n 50

# В реальном времени
journalctl -u ekzor_comments_autosend -f

# Или через файл
tail -f /var/log/ekzor_comments_autosend.log
```

---

## 🧪 Тестирование

1. Убедитесь, что бот запущен:
```bash
systemctl status ekzor_comments_autosend
```

2. Опубликуйте тестовый пост в канале

3. Проверьте, что бот отправил комментарий с картинкой и кнопками

4. Проверьте логи на наличие ошибок:
```bash
journalctl -u ekzor_comments_autosend -n 20
```

---

## 🔍 Решение проблем

### Бот не отвечает на посты

1. **Проверьте, что бот запущен:**
```bash
systemctl status ekzor_comments_autosend
```

2. **Проверьте логи:**
```bash
journalctl -u ekzor_comments_autosend -n 50
```

3. **Убедитесь, что бот добавлен в канал как администратор**

4. **Проверьте, что у бота есть права на публикацию сообщений**

5. **Проверьте правильность CHANNEL_ID:**
```bash
cat /opt/ekzor_comments_autosend/.env | grep CHANNEL_ID
```

### Бот падает при запуске

1. **Проверьте корректность токена:**
```bash
cat /opt/ekzor_comments_autosend/.env | grep BOT_TOKEN
```

2. **Проверьте, что установлены зависимости:**
```bash
/opt/ekzor_comments_autosend/venv/bin/pip list | grep telegram
```

3. **Запустите вручную для просмотра ошибок:**
```bash
cd /opt/ekzor_comments_autosend
source venv/bin/activate
python3 ekzor_comments_autosend.py
```

### Комментарий отправляется без картинки

1. **Проверьте путь к картинке:**
```bash
ls -la /opt/ekzor_comments_autosend/image.jpg
```

2. **Проверьте права на файл:**
```bash
chown telegram-bot:telegram-bot /opt/ekzor_comments_autosend/image.jpg
```

3. **Проверьте размер картинки (должна быть < 10MB)**

### Комментарии не прикрепляются к постам

1. **Убедитесь, что в канале включены комментарии (Discussion Group)**

2. Перейдите в настройки канала → Обсуждения → Создайте/привяжите группу

---

## 📝 Дополнительные возможности

### Изменение текста комментария

Вы можете использовать HTML-разметку в тексте комментария:

```bash
COMMENT_TEXT=<b>Жирный текст</b>
🎵 <i>Курсив</i>
<a href="https://example.com">Ссылка</a>
```

### Добавление больше кнопок

Отредактируйте файл `ekzor_comments_autosend.py`, найдите секцию с клавиатурой:

```python
keyboard = [
    [
        InlineKeyboardButton("💬 Чат", url=CHAT_URL),
        InlineKeyboardButton("🎵 Яндекс Музыка", url=MUSIC_URL)
    ],
    [
        InlineKeyboardButton("🌐 Сайт", url="https://your-site.com"),
        InlineKeyboardButton("📱 Instagram", url="https://instagram.com/your")
    ]
]
```

### Мониторинг нескольких каналов

Создайте отдельный экземпляр бота для каждого канала:

```bash
# Скопируйте директорию
cp -r /opt/ekzor_comments_autosend /opt/ekzor_comments_autosend_channel2

# Создайте отдельный service
cp /etc/systemd/system/ekzor_comments_autosend.service /etc/systemd/system/ekzor_comments_autosend_channel2.service

# Отредактируйте пути в service
# Запустите второй бот
systemctl start ekzor_comments_autosend_channel2
```

---

## 🔒 Безопасность

1. **Никогда не публикуйте токен бота**
2. **Ограничьте права бота только необходимыми**
3. **Регулярно проверяйте логи на подозрительную активность**
4. **Используйте отдельного пользователя (telegram-bot) для запуска**

---

## 📞 Структура файлов

```
/opt/ekzor_comments_autosend/
├── ekzor_comments_autosend.py  # Основной файл бота
├── requirements.txt             # Зависимости Python
├── .env                         # Конфигурация (секретные данные)
├── image.jpg                    # Картинка для комментариев
└── venv/                        # Виртуальное окружение Python

/etc/systemd/system/
└── ekzor_comments_autosend.service  # Systemd service файл

/var/log/
└── ekzor_comments_autosend.log      # Логи бота
```

---

## 📌 Полезные команды

```bash
# Быстрая перезагрузка после изменений
systemctl restart ekzor_comments_autosend && journalctl -u ekzor_comments_autosend -f

# Проверка всех логов за сегодня
journalctl -u ekzor_comments_autosend --since today

# Очистка логов (если занимают много места)
journalctl --vacuum-time=7d

# Проверка использования ресурсов ботом
systemctl status ekzor_comments_autosend
```

---

## ✅ Чек-лист перед запуском

- [ ] Создан бот через @BotFather
- [ ] Бот добавлен в канал как администратор
- [ ] У бота есть права на публикацию сообщений
- [ ] В канале включены комментарии (Discussion Group)
- [ ] Файл .env заполнен правильными данными
- [ ] Картинка image.jpg загружена
- [ ] Python зависимости установлены
- [ ] Systemd service настроен и запущен
- [ ] Бот добавлен в автозагрузку

---

## 🎉 Готово!

Теперь ваш бот будет автоматически отправлять комментарии под каждый новый пост в канале!

При возникновении проблем проверьте логи:
```bash
journalctl -u ekzor_comments_autosend -f
```
